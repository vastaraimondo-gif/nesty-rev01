export function priceToScore(delta){ if(delta<=-0.10)return 10; if(delta<=0.05)return 8.5; if(delta<=0.15)return 6.5; return 4.5;}
export function calculateRoomScore(i){const med=i.medianRoomPrice||700; const delta=(i.price-med)/med; const ps=priceToScore(delta); const clarity=i.expensesIncluded?8.8:6.8; const score=ps*.30+i.mobilityScore*.15+i.urbanQualityIndex*.15+i.servicesScore*.10+clarity*.10+i.youthFitScore*.20; return {score:Number(score.toFixed(1)),delta,priceScore:ps,listingClarityScore:clarity};}
export function calculateApartmentScore(i){const est=i.euroMq*i.sizeMq; const delta=(i.rent-est)/est; const ps=priceToScore(delta); const score=ps*.35+i.mobilityScore*.15+i.urbanQualityIndex*.15+i.servicesScore*.15+i.trendScore*.10+i.profileFitScore*.10; return {score:Number(score.toFixed(1)),estimated:Math.round(est),delta,priceScore:ps};}
export function scoreLabel(score){ if(score>=8.5)return 'Ottima scelta'; if(score>=7.5)return 'Buona scelta'; if(score>=6.5)return 'Da valutare'; return 'Attenzione';}
